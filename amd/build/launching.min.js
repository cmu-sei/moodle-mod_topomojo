define("mod_topomojo/launching",["jquery","core/log"],(function($,log){"use strict";return{init:function(params){var refreshInterval=params.refreshInterval||5,maxAttempts=params.maxAttempts||24,attemptCount=0;log.debug("mod_topomojo/launching: Starting auto-refresh (interval: "+refreshInterval+"s, max attempts: "+maxAttempts+")");var refreshTimer=setInterval((function(){attemptCount++,log.debug("mod_topomojo/launching: Refresh attempt "+attemptCount+" of "+maxAttempts),attemptCount>=maxAttempts&&(clearInterval(refreshTimer),log.debug("mod_topomojo/launching: Max attempts reached, stopping auto-refresh")),window.location.reload()}),1e3*refreshInterval)}}}));

//# sourceMappingURL=launching.min.js.map
