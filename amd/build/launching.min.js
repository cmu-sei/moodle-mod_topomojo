define(["jquery","core/log"],function($,log){"use strict";return{init:function(params){var refreshInterval=params.refreshInterval||5;var maxAttempts=params.maxAttempts||24;var attemptCount=0;log.debug("mod_topomojo/launching: Starting auto-refresh (interval: "+refreshInterval+"s, max attempts: "+maxAttempts+")");var refreshTimer=setInterval(function(){attemptCount++;log.debug("mod_topomojo/launching: Refresh attempt "+attemptCount+" of "+maxAttempts);window.location.reload();if(attemptCount>=maxAttempts){clearInterval(refreshTimer);log.debug("mod_topomojo/launching: Max attempts reached, stopping auto-refresh")}},refreshInterval*1000)}}});
//# sourceMappingURL=launching.min.js.map
